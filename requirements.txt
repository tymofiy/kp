# KP:1 conformance suite Python dependencies
# Install with: pip install -r requirements.txt
pyyaml>=6.0.3
# format-nongpl pulls the non-GPL format validators (rfc3339-validator,
# rfc3986-validator, ...) so JSON Schema "format" assertions (date,
# date-time, uri) are actually enforced. Bare jsonschema silently skips
# any format it has no validator for.
jsonschema[format-nongpl]>=4.26
# PEG engine for `run.py --strict`, which machine-executes the normative
# grammar/kp-claims.peg. Chosen for faithful PEG semantics (ordered
# choice, and/not predicates) — see the strict layer in conformance/run.py.
parsimonious>=0.11
# SQLite vector search extension for the experimental graph compiler's
# derived vector index. Pre-v1: keep this range tight until the vec0
# table contract is stable enough for broader compatibility.
sqlite-vec>=0.1.9,<0.2
